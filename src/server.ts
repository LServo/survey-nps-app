/**
 * GET => BUSCA
 * POST => SALVAR
 console.log('SALVAR:', SALVAR)
 * PUT => ALTERAR
 * DELETE => DELETAR
 * PATCH => ALTERAÇÃO ESPECÍFICA
 * 
 * o controller que é o responsável por chamar todas as execuções e regras de negócio da aplicação
 */

import { app } from "./app";

app.listen(3000, () => console.log('Server running on port 3000'))